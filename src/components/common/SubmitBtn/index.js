import "./style.css";

export default function SubmitBtn() {
  return (
    <div className="submit">
      <button onClick={} className="word">
        
        Submit
      </button>
    </onClick={} );
}
